[tool.poetry]
name = "poetry-smoke"
version = "0.1.0"
description = "sbox poetry smoke project"
authors = ["sbox <sbox@example.invalid>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"
rich = "^14.0.0"

[build-system]
requires = ["poetry-core>=1.9.0"]
build-backend = "poetry.core.masonry.api"
